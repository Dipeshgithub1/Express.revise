//npm is standard package manager for Node.js //its manages download of dependecies of ur project.

//shortcut of the flags can also be used
// S = --save
//-D == --save-dev
//O == --save-optinal

//devDependencies and dependencies

{
    "script": {
        "start-dev":"node lib/server-develooment",
        "start": "node lib/server-production"
    }
}